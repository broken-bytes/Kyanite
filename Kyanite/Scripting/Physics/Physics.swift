import Math

public struct Hit {
    var point: Vector3
}

class Physics3D {
    public static func raycast(from: Vector3, to: Vector3) -> Hit? {
        return nil
    }

    public static func raycast(from: Vector3, direction: Vector3, distance: Float) -> Hit? {
        return nil
    }

    public static func set(gravity: Vector3) {
        
    }
}